# Legacy notebooks

These notebooks are kept for reference only. They power the original
notebook-based Whisper + pyannote workflow that the new `meetings` package
supersedes. Do not extend them; modify the package under `src/meetings/` instead.

- `diarize_transcribe.ipynb` — original combined Whisper + pyannote pipeline.
- `check-transcribe.ipynb` — exploratory transcription / spectrogram analysis.
- `whisper-notebook_T4output.ipynb` — Colab T4 run output (large file).
